# App Configuration
APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Database (PostgreSQL — use Neon or Vercel Postgres for free tier)
DATABASE_URL="postgresql://user:password@host:5432/socialpulse?sslmode=require"

# Meta (Facebook/Instagram) API
# Create app at: https://developers.facebook.com/
META_CLIENT_ID=""
META_CLIENT_SECRET=""
META_REDIRECT_URI="${APP_URL}/api/connect/meta/callback"

# YouTube Data & Analytics API
# Create OAuth 2.0 credentials at: https://console.cloud.google.com/
# Enable: YouTube Data API v3 & YouTube Analytics API
YOUTUBE_CLIENT_ID=""
YOUTUBE_CLIENT_SECRET=""
YOUTUBE_REDIRECT_URI="${APP_URL}/api/connect/youtube/callback"

# Gemini AI API (optional — for AI-powered insights)
# Get key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# Admin Auth (HTTP Basic Auth — protects all routes)
# Set a strong password — this is your only login gate
ADMIN_PASSWORD="changeme"
